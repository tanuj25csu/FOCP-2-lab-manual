#include <iostream>
using namespace std;
int main() {
    int rad;
    float area, circum;
    cout << "enter the radius" << endl;
    cin >> rad;
    area = 3.14 * rad * rad;
    cout << "The area of the circle is " << area << endl;
    return 0;
}
